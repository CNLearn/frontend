import { createStore } from 'vuex';
import search from '@/store/modules/search';

export default createStore({
  modules: {
    search,
  },
  state: () => ({}),
  mutations: {
  },
  actions: {
  },
});
